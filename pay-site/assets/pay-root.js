/* Редирект с корня поддомена в кабинет. Дублирует meta refresh:
   его игнорируют некоторые встроенные браузеры.
   Вынесено из разметки под строгий CSP. */
// Дублируем редирект скриптом: meta refresh игнорируют некоторые встроенные
// браузеры, а человек не должен упираться в промежуточную страницу.
window.location.replace('/account/');
